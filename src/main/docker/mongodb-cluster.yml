version: '2'
services:
  jhipstersamplemongo-mongodb:
    image: mongo:4.2.3
    ports:
      - '27017:27017'
    command: mongos --configdb jhipstersamplemongo-mongodb-config
  jhipstersamplemongo-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  jhipstersamplemongo-mongodb-config:
    image: mongo:4.2.3
    container_name: jhipstersamplemongo-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
